# Core API server deps (main.py, cici_driver.py)
fastapi>=0.110
uvicorn[standard]>=0.27
pyyaml>=6.0
httpx>=0.27
playwright>=1.40

# CLI deps (cici/) — đã liệt kê trong pyproject.toml, lặp lại đây cho ai chỉ pip install -r
click>=8.1
rich>=13.7
